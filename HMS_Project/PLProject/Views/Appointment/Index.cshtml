@model IEnumerable<ApointmentViewModel>
@{
    ViewData["Title"] = "View Appointments";
}


<h1>All Appointments</h1>
<br />
<br />

<a asp-controller="Appointment" asp-action="Create" class="btn btn-primary">Create New Appointment</a>

<br />
@if (Model.Count() > 0)
{
    @* <table class="mt-4 table table-striped table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(a => a.)</th>
                <th>@Html.DisplayNameFor(C => C.Phone)</th>
                <th>@Html.DisplayNameFor(C => C.Specialization)</th>
                <th>@Html.DisplayNameFor(C => C.Price)</th>
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var clinic in Model)
            {
                <tr>
                    <td class="align-content-center">@clinic.Name</td>
                    <td class="align-content-center">@clinic.Phone</td>
                    <td class="align-content-center">@clinic.Specialization</td>
                    <td class="align-content-center">@clinic.Price</td>
                    <td>
                        <a asp-controller="Department" asp-action="Details" asp-route-Id="@clinic.Id" class="btn btn-warning">
                            <i class="fas fa-eye"></i> Details
                        </a>
                    </td>
                    <td>
                        <a asp-controller="Department" asp-action="Edit" asp-route-Id="@clinic.Id" class="btn btn-success">
                            <i class="fas fa-edit"></i> Update
                        </a>
                    </td>
                    <td>
                        <a asp-controller="Department" asp-action="Delete" asp-route-Id="@clinic.Id" class="btn btn-danger">
                            <i class="fas fa-trash"></i> Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table> *@
}
else
{
    <div class="mt-4 alert alert-warning">
        <h3>There is no Appointments!</h3>
    </div>
}