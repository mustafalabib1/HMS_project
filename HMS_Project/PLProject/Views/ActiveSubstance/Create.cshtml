@model ActiveSubstanceViewModel
@inject IUnitOfWork unitOfWork
@{
    var ActiveSubstancesDateReader = unitOfWork.Repository<ActiveSubstance>().GetALL().ToList();
    ViewData["Title"] = "Create New Active Substance";
}
<div class="container mt-5">
    <h2 class="mb-4">Create New Active Substance</h2>

    <form asp-action="Create" method="post">

        <!-- Active Substance Name -->
        <div class="form-group">
            <label asp-for="ActiveSubstancesName" class="fw-bold">Active Substance Name</label>
            <input asp-for="ActiveSubstancesName" class="form-control form-control-lg" placeholder="Enter active substance name" />
            <span asp-validation-for="ActiveSubstancesName" class="text-danger"></span>
        </div>

        <!-- Dynamic Interactions Section -->
        <div class="form-group mt-4">
            <label class="fw-bold">Active Substance Interactions (optional):</label>
            <div id="interactions-list" class="row gy-3">
                @for (int i = 0; i < Model.Interactions.Count; i++)
                {
                    <div class="interaction-item col-md-12 row align-items-end mb-3">
                        <div class="col-md-6">
                            <label class="d-block">Select Substance</label>
                            <select asp-for="@Model.Interactions[i].ActSubId" class="form-control form-control-lg">
                                <option value="">Select Substance</option>
                                @foreach (var substance in ActiveSubstancesDateReader)
                                {
                                    <option value="@substance.Id">@substance.ActiveSubstancesName</option>
                                }
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="@Model.Interactions[i].Interaction" class="d-block">Interaction</label>
                            <input asp-for="@Model.Interactions[i].Interaction" class="form-control form-control-lg" placeholder="Enter interaction" />
                        </div>
                        <div class="col-md-2 d-flex align-items-center">
                            <button type="button" class="btn btn-danger btn-lg remove-interaction" title="Remove Interaction">
                                <i class="fas fa-trash-alt"></i>
                            </button>
                        </div>
                    </div>
                }
            </div>
            <button type="button" class="btn btn-success btn-lg mt-3" id="add-interaction">
                <i class="fas fa-plus-circle"></i> Add Interaction
            </button>
        </div>

        <!-- Medications Section -->
        <div class="form-group mt-4">
            <label class="fw-bold">Add new Medications (optional):</label>
            <select asp-for="MedicationId" class="form-control select2-multiple form-control-lg" multiple="multiple" data-placeholder="Select medications">
                @foreach (var medication in Model.MedicationsDateReader)
                {
                    <option value="@medication.Id">@medication.MedName</option>
                }
            </select>
        </div>

        <!-- Submit Button -->
        <div class="mt-4 d-flex justify-content-between">
            <button type="submit" class="btn btn-primary btn-lg">
                <i class="fas fa-save"></i> Submit
            </button>
            <a asp-action="Index" class="btn btn-secondary btn-lg">
                <i class="fas fa-arrow-left"></i> Back to List
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/select2/js/select2.min.js"></script>
    <link href="~/select2/css/select2.min.css" rel="stylesheet" />

    <script>
        $(document).ready(function () {
            // Initialize Select2 for Medication field
            $('.select2-multiple').select2({
                placeholder: "Select from the list",
                allowClear: true,
                width: '100%'
            });

                // Add new Interaction Row
                    $('#add-interaction').click(function () {
                        var index = $('.interaction-item').length;
                        var newInteraction = `
                            <div class="interaction-item col-md-12 row align-items-end mb-3">
                                <div class="col-md-6">
                                    <label class="d-block">Select Substance</label>
                                    <select name="Interactions[` + index + `].ActSubId" class="form-control form-control-lg">
                                        <option value="">Select Substance</option>
        @foreach (var substance in ActiveSubstancesDateReader)
        {
                                                <option value="@substance.Id">@substance.ActiveSubstancesName</option>
        }
                                        }
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <label class="d-block">Interaction</label>
                                    <input type="text" name="Interactions[` + index + `].Interaction" class="form-control form-control-lg" placeholder="Enter interaction" />
                                </div>
                                <div class="col-md-2 d-flex align-items-center">
                                    <button type="button" class="btn btn-danger btn-lg remove-interaction">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>
                                </div>
                            </div>
                        `;
                        $('#interactions-list').append(newInteraction).hide().fadeIn(300); // Smooth animation, append at the bottom
                    });

                    // Remove Interaction Row
                    $(document).on('click', '.remove-interaction', function () {
                        $(this).closest('.interaction-item').fadeOut(300, function () {
                            $(this).remove(); // Smooth removal
                        });
                    });
                });
    </script>
}
